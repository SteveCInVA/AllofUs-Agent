[pytest]
testpaths = tests
markers =
    live: hits real external services over the network; deselected by default
    slow: slow tests (e.g. the CCDI federation API)
    smoke: end-to-end pipeline smoke test
addopts = -m "not live"
